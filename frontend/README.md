# Frontend — AQUASHIELD

React + TypeScript + Vite client. Owns UI, 3D visualization, and client-side state.
Does not contain simulation, agent, or RAG logic — it consumes them through shared/ contracts and backend/ APIs.

See src/ for the domain breakdown (app, components, features, three, animations, hooks, stores, services, api, types, utils, constants, styles).
