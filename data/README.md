# Data — AQUASHIELD

Environmental/geospatial data. raw/, processed/, and external/ are gitignored (see root .gitignore) — do not commit datasets. fixtures/, schemas/, and samples/ stay small and tracked.
