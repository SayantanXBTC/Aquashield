# Simulation — AQUASHIELD

Disaster-agnostic physics/numerical simulation engine. Owns the DisasterScenario abstraction (see architecture.md §6) and every disaster model. The AI agents interpret this engine's output; they never compute physics themselves.
